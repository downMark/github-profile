# Local development only. Copy this file to app/.env and replace the placeholder.
DB_NAME=postgres
DB_USERNAME=postgres
DB_PASSWORD=postgres
TOKEN_ENCRYPTION_KEY=replace-with-64-hex-characters

# Auth Service 的 RSA 密钥在本地启动时临时生成，不要在这里保存私钥。
